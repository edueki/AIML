# AI Course Enrollment Chatbot

This is a Streamlit UI for the AI Course Enrollment system.  
It connects to your Orchestrator API to handle user queries, tool orchestration, and results.

## Setup
1. Clone the repo: `git clone <repo_url>`
2. Navigate: `cd streamlit_chatbot`
3. Create and activate a virtualenv:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
